// Eddystrap - Natural kinetic UI components for the web
import './styles.css';
import { Eddy, type EddyOptions } from './Eddy.js';
import { Pool, type PoolOptions } from './Pool.js';

export { Eddy, Pool };
export type { EddyOptions, PoolOptions };

// Default export for convenience
export default {
  Eddy,
  Pool
};
